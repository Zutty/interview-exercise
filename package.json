{
  "name": "web-dev-exercise",
  "version": "1.0.0",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "serve": "esbuild src/index.js --bundle --outdir=dist --watch --servedir=.",
    "test": "node --test"
  },
  "devDependencies": {
    "esbuild": "^0.20.2",
    "prettier": "^3.2.5"
  },
  "dependencies": {
    "konva": "^9.3.6",
    "polylabel": "^1.1.0"
  }
}
